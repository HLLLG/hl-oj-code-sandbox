spring:
  application:
    name: hloj-code-sandbox
  jackson:
    parser:
      allow-unquoted-control-chars: true
server:
  port: 8090

code-sandbox:
  # native | docker
  type: docker
#  docker:
#    # 远端 VMware 中的 Docker daemon（需在 VM 内开启 2375 远程 API，无 TLS 仅限可信内网）
#    host: tcp://10.122.6.63:2375
#    image: hub.cloud.ctripcorp.com/devops/almalinux-java21-maven3.8.8:latest
#    memory-mb: 128
#    cpu-count: 1
  docker:
    # 个人 VMware 虚拟机 Docker daemon（2375，仅可信内网）
    host: tcp://192.168.184.128:2375
    image: eclipse-temurin:21-jre
    memory-mb: 128
    cpu-count: 1
